<!-- A DrawerLayout is intended to be used as the top-level content view using match_parent for both width and height to consume the full space available. -->
<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:ads="http://schemas.android.com/apk/res-auto"
	xmlns:fab="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="ca.josephroque.uottawacampusnavigator.NavigationActivity">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
		
		<include
			layout="@layout/toolbar" />

        <com.google.android.gms.ads.AdView
            android:id="@+id/navigation_bottom_banner_ad"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_alignParentBottom="true"
            android:visibility="gone"
            ads:adSize="SMART_BANNER"
            ads:adUnitId="@string/navigation_bottom_unit_id" />

        <FrameLayout
            android:id="@+id/container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
			android:layout_below="@id/toolbar"
			android:layout_above="@id/navigation_bottom_banner_ad"/>
		
		<!-- TODO: add preference to disable floating action button for search -->
		<com.melnykov.fab.FloatingActionButton
			android:id="@+id/fab_navigation_search"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:layout_alignParentBottom="true"
			android:layout_alignParentRight="true"
			android:layout_alignParentEnd="true"
			android:layout_margin="@dimen/floatingactionbutton_margin"
			fab:fab_colorNormal="@color/primary_garnet"
			fab:fab_colorPressed="@color/primary_garnet"
			fab:fab_colorRipple="@color/primary_garnet_dark" />

    </RelativeLayout>

    <!-- The drawer is given a fixed width in dp and extends the full height of
         the container. -->
    <fragment android:id="@+id/navigation_drawer"
              android:layout_width="@dimen/navigation_drawer_width"
              android:layout_height="match_parent"
              android:layout_gravity="left"
              android:name="ca.josephroque.uottawacampusnavigator.fragment.NavigationDrawerFragment"
              tools:layout="@layout/fragment_navigation_drawer"
              tools:ignore="RtlHardcoded"/>

</android.support.v4.widget.DrawerLayout>
